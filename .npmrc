# This flattens dependencies to the root node_modules, 
# making it easier for Metro/Gradle/CocoaPods to find them.
node-linker=hoisted

# Alternatively, if you want stricter isolation but riskier Native builds:
# node-linker=isolated
# (I strongly recommend 'hoisted' for React Native to avoid build failures)